core 모듈 생성 명령어

### Android Library로 data 모듈 생성
./create-core-module.sh data android

### 순수 Kotlin으로 model 모듈 생성
./create-core-module.sh model kotlin

### 순수 Kotlin으로 utils 모듈 생성
./create-core-module.sh utils kotlin

### Android Library로 ui 모듈 생성 (기본값)
./create-core-module.sh ui
